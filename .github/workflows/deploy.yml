name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      target:
        description: "Force-deploy a specific worker (or 'all')"
        required: false
        default: ""
        type: choice
        options:
          - ""
          - all
          - control-plane
          - sandbox-api
          - sandbox-dispatch
          - dispatch-worker
          - tail-worker
          - realtime-worker
          - deploy-api
          - mcp-server
          - remote-builder

# Run deploys serially for safety — no concurrent prod pushes.
concurrency:
  group: deploy-prod
  cancel-in-progress: false

jobs:
  changes:
    runs-on: ubuntu-latest
    outputs:
      control-plane: ${{ steps.filter.outputs.control-plane }}
      sandbox-api: ${{ steps.filter.outputs.sandbox-api }}
      sandbox-dispatch: ${{ steps.filter.outputs.sandbox-dispatch }}
      dispatch-worker: ${{ steps.filter.outputs.dispatch-worker }}
      tail-worker: ${{ steps.filter.outputs.tail-worker }}
      realtime-worker: ${{ steps.filter.outputs.realtime-worker }}
      deploy-api: ${{ steps.filter.outputs.deploy-api }}
      mcp-server: ${{ steps.filter.outputs.mcp-server }}
      remote-builder: ${{ steps.filter.outputs.remote-builder }}
    steps:
      - uses: actions/checkout@v6
      - uses: dorny/paths-filter@v4
        id: filter
        with:
          filters: |
            control-plane:
              - 'packages/control-plane/**'
              - 'packages/sdk/**'
              - 'packages/deploy-core/**'
            sandbox-api:
              - 'packages/sandbox-api/**'
              - 'packages/sdk/**'
              - 'packages/deploy-core/**'
            sandbox-dispatch:
              - 'packages/sandbox-dispatch/**'
            dispatch-worker:
              - 'packages/dispatch-worker/**'
            tail-worker:
              - 'packages/tail-worker/**'
            realtime-worker:
              - 'packages/realtime-worker/**'
            deploy-api:
              - 'packages/deploy-api/**'
            mcp-server:
              - 'packages/mcp-server/**'
            remote-builder:
              - 'packages/remote-builder/**'
              - 'packages/build-container/**'

  deploy-control-plane:
    needs: changes
    if: ${{ (github.event_name == 'push' && needs.changes.outputs.control-plane == 'true') || (github.event_name == 'workflow_dispatch' && (inputs.target == 'control-plane' || inputs.target == 'all')) }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: '22'
      - uses: pnpm/action-setup@v5
      - run: pnpm install
      - run: pnpm turbo build --filter=@solcreek/control-plane
      - name: Deploy
        run: pnpm --filter @solcreek/control-plane exec wrangler deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}

  deploy-sandbox-api:
    needs: changes
    if: ${{ (github.event_name == 'push' && needs.changes.outputs.sandbox-api == 'true') || (github.event_name == 'workflow_dispatch' && (inputs.target == 'sandbox-api' || inputs.target == 'all')) }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: '22'
      - uses: pnpm/action-setup@v5
      - run: pnpm install
      - run: pnpm turbo build --filter=@solcreek/sandbox-api
      - name: Deploy
        run: pnpm --filter @solcreek/sandbox-api exec wrangler deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}

  deploy-sandbox-dispatch:
    needs: changes
    if: ${{ (github.event_name == 'push' && needs.changes.outputs.sandbox-dispatch == 'true') || (github.event_name == 'workflow_dispatch' && (inputs.target == 'sandbox-dispatch' || inputs.target == 'all')) }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: '22'
      - uses: pnpm/action-setup@v5
      - run: pnpm install
      - run: pnpm turbo build --filter=@solcreek/sandbox-dispatch
      - name: Deploy
        run: pnpm --filter @solcreek/sandbox-dispatch exec wrangler deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}

  deploy-dispatch-worker:
    needs: changes
    if: ${{ (github.event_name == 'push' && needs.changes.outputs.dispatch-worker == 'true') || (github.event_name == 'workflow_dispatch' && (inputs.target == 'dispatch-worker' || inputs.target == 'all')) }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: '22'
      - uses: pnpm/action-setup@v5
      - run: pnpm install
      - run: pnpm turbo build --filter=@solcreek/dispatch-worker
      - name: Deploy
        run: pnpm --filter @solcreek/dispatch-worker exec wrangler deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}

  # creek-tail is the Tail Worker attached to creek-dispatch's
  # `tail_consumers`, so it collects trace events for the dispatch worker
  # AND every user worker dispatched through it — i.e. it is the whole
  # observability pipeline behind `creek logs` and `creek metrics`.
  #
  # It was missing from this workflow entirely until 2026-07-30. Merging a
  # tail-worker change looked completely healthy — green CI, green Deploy
  # run — while every job silently skipped, because no filter matched. PR
  # #35 sat on main for a deploy cycle while production kept running a
  # build from 2026-04-13.
  deploy-tail-worker:
    needs: changes
    if: ${{ (github.event_name == 'push' && needs.changes.outputs.tail-worker == 'true') || (github.event_name == 'workflow_dispatch' && (inputs.target == 'tail-worker' || inputs.target == 'all')) }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: '22'
      - uses: pnpm/action-setup@v5
      - run: pnpm install
      - run: pnpm turbo build --filter=@solcreek/tail-worker
      - name: Deploy
        run: pnpm --filter @solcreek/tail-worker exec wrangler deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}

  deploy-realtime-worker:
    needs: changes
    if: ${{ (github.event_name == 'push' && needs.changes.outputs.realtime-worker == 'true') || (github.event_name == 'workflow_dispatch' && (inputs.target == 'realtime-worker' || inputs.target == 'all')) }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: '22'
      - uses: pnpm/action-setup@v5
      - run: pnpm install
      - run: pnpm turbo build --filter=@solcreek/realtime-worker
      - name: Deploy
        run: pnpm --filter @solcreek/realtime-worker exec wrangler deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}

  deploy-deploy-api:
    needs: changes
    if: ${{ (github.event_name == 'push' && needs.changes.outputs.deploy-api == 'true') || (github.event_name == 'workflow_dispatch' && (inputs.target == 'deploy-api' || inputs.target == 'all')) }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: '22'
      - uses: pnpm/action-setup@v5
      - run: pnpm install
      - run: pnpm turbo build --filter=@solcreek/deploy-api
      - name: Deploy
        run: pnpm --filter @solcreek/deploy-api exec wrangler deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}

  deploy-mcp-server:
    needs: changes
    if: ${{ (github.event_name == 'push' && needs.changes.outputs.mcp-server == 'true') || (github.event_name == 'workflow_dispatch' && (inputs.target == 'mcp-server' || inputs.target == 'all')) }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: '22'
      - uses: pnpm/action-setup@v5
      - run: pnpm install
      - run: pnpm turbo build --filter=@solcreek/mcp-server
      - name: Deploy
        run: pnpm --filter @solcreek/mcp-server exec wrangler deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}

  # remote-builder uses Cloudflare Containers (Docker image push to CF
  # registry). API token needs Containers: Edit + Cloudchamber: Edit
  # in addition to Workers Scripts: Edit.
  deploy-remote-builder:
    needs: changes
    if: ${{ (github.event_name == 'push' && needs.changes.outputs.remote-builder == 'true') || (github.event_name == 'workflow_dispatch' && (inputs.target == 'remote-builder' || inputs.target == 'all')) }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: '22'
      - uses: pnpm/action-setup@v5
      - run: pnpm install
      - run: pnpm turbo build --filter=@solcreek/build-container --filter=@solcreek/remote-builder
      - name: Deploy
        run: pnpm --filter @solcreek/remote-builder exec wrangler deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
