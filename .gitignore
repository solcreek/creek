node_modules/
dist/
.wrangler/
.turbo/
*.tsbuildinfo
.env
.env.local
.env.production
.dev.vars

# wrangler configs are committed — Cloudflare account/resource IDs are
# not classified as secrets by CF (only API tokens and `[secrets]` are).
# Real secrets are set via `wrangler secret put` and never appear in files.

# Internal planning docs
product-planning/

# Local-only infrastructure / operations notes (account IDs, resource IDs, etc.)
*.local.md

# Build output
.next/
.open-next/
.source/
.creek/
.claude/
