{
  "name": "@solcreek/mcp-server",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "sync-refs": "rm -rf .skills-tmp && git clone --depth 1 https://github.com/solcreek/skills .skills-tmp && cp .skills-tmp/skills/creek/references/*.md references/ && rm -rf .skills-tmp",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "hono": "^4.7.4",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260405.1",
    "msw": "^2.12.14",
    "typescript": "^5.8.2",
    "wrangler": "^4.82.2"
  }
}
